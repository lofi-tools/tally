{
  "name": "@tally/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "panda codegen && panda cssgen",
    "dev": "vite",
    "prebuild": "panda codegen && panda cssgen",
    "build": "vite build",
    "preview": "vite preview",
    "codegen": "panda codegen && panda cssgen",
    "typecheck": "panda codegen && panda cssgen && tsc --noEmit",
    "smoke": "vite build && node scripts/smoke.mjs",
    "flow": "vite build && node scripts/flow.mjs"
  },
  "dependencies": {
    "@fontsource-variable/outfit": "^5.3.0",
    "@tally/design-system": "workspace:*",
    "lucide-solid": "^1.30.0",
    "solid-js": "^1.9.14"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.12.0",
    "@pandacss/preset-panda": "^1.12.0",
    "@types/node": "^24.0.0",
    "jsdom": "^30.0.1",
    "postcss": "^8.5.0",
    "typescript": "~5.9.3",
    "vite": "^8.2.1",
    "vite-plugin-solid": "^2.11.14"
  }
}
