{
  "name": "tally",
  "private": true,
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "prepare": "pnpm -r codegen",
    "dev": "pnpm --filter @tally/web dev",
    "build": "pnpm --filter @tally/web build",
    "preview": "pnpm --filter @tally/web preview",
    "codegen": "pnpm -r codegen",
    "typecheck": "pnpm -r typecheck"
  }
}
