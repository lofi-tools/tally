//! Never-compiled stub for `toasty-driver-sqlite`.
//!
//! tally-api uses only the `postgresql` toasty driver, so this crate exists
//! solely to satisfy lockfile resolution (see the sibling `Cargo.toml` for
//! the full rationale).
